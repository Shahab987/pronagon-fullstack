name: deploy-union
on:
  push:
    branches: [master]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v2
      
      - name: Setup Node.js
        uses: actions/setup-node@v1
        with:
          node-version: "18"
          
      - name: Build Frontend
        run: |
          npm install
          npm run build

      - name: Copy Build Files to Server Directory
        run: |
          mkdir -p server
          cp -r dist/* server/

      - name: Commit and Push to New Repository
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TARGET_REPO: Shahab987/phonegon-union
          TARGET_BRANCH: main
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git remote remove origin || true  # Remove existing remote, ignore errors if it doesn't exist
          git remote add origin https://x-access-token:${GITHUB_TOKEN}@github.com/${TARGET_REPO}.git
          git add .
          git commit -m "Update built files"
          git push -u origin $TARGET_BRANCH
